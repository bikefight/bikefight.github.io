services:
  - type: web
    name: bikefight
    env: node
    plan: starter
    buildCommand: "npm install"
    startCommand: "node server.js"
    healthCheckPath: /

disks:
  - name: data
    mountPath: /data
    sizeGB: 1 